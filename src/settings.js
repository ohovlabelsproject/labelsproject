const settings = {
  modes: {
    testMode: false,
  },
  shouldSkipWelcome: {
    onLabelSubmission: false,
  },
  labels: {
    minLength: 3,
    maxLength: 25,
    shouldAddBinOnSubmission: true,
  },
};

export default settings;
