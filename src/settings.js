const settings = {
  modes: {
    testMode: false,
  },
  shouldSkipWelcome: {
    onLabelSubmission: false,
  },
  labels: {
    minLength: 3,
    maxLength: 20,
    shouldAddBinOnSubmission: true,
  },
};

export default settings;
