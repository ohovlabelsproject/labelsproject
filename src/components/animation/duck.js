function Duck() {
  return (
    <div
      id="duck"
      className="duck"
      style={{ background: "#000", color: "#fff", display: "none" }}
    >
      Duck truck here
    </div>
  );
}

export default Duck;
